---
title: "Lineare Optimierung mit dem Simplex-Verfahren"
subtitle: "Ein praktisches Einführungsbeispiel"
author: Prof. Dr. Stefan Böcker, FRM
date: \today
toc: false
output: fhswf::presentation
knit: fhswf::render_presentation
german: true # German Dummy Text
lang: de-de   # Text Language: en-gb, en-us, de-de
---

```{r setup, include=FALSE}
options(digits = 4)
knitr::opts_chunk$set(echo=FALSE, message=FALSE, warning=FALSE, cache=FALSE, 
                      dev.args=list(pointsize=11), size="scriptsize")
library(tidyverse)
library(gridExtra)
library(knitr)
```

## Inhaltsübersicht {.build}

- **Das Optimierungsproblem**: Motivation und Formulierung
- **Standardform**: Umwandlung zu Standardform
- **Das Simplex-Tableau**: Aufbau und Interpretation
- **Der Simplex-Algorithmus**: Schritt-für-Schritt Verfahren
- **Praktisches Beispiel**: Vollständige Durchrechnung
- **Visualisierung**: Geometrische Interpretation

---

## Das Optimierungsproblem

### Klassisches Problem: Produktionsprogrammplanung

Ein Unternehmen produziert zwei Produkte:
- **Produkt A**: Gewinn 40 EUR pro Einheit
- **Produkt B**: Gewinn 30 EUR pro Einheit

**Ressourcen-Beschränkungen:**
- Arbeitszeit: max. 40 Stunden (4h für A, 2h für B)
- Materialverfügbarkeit: max. 120 kg (10 kg für A, 30 kg für B)
- Lagerkapazität: max. 9 Einheiten insgesamt

**Ziel**: Gewinn maximieren

```{r echo=TRUE}
# Zielfunktion und Nebenbedingungen
cat("Maximiere: Z = 40x_1 + 30x_2\n")
cat("\nNebenbedingungen:\n")
cat("4$x_1$ + 2$x_2$ <= 40    (Arbeitszeit)\n")
cat("10$x_1$ + 30$x_2$ <= 120  (Material)\n")
cat("$x_1 + x_2 <= 9$        (Lager)\n")
cat("$x_1, x_2 >= 0$         (Nicht-Negativität)\n")
```

---

## Umwandlung zur Standardform {.build}

### Schritt 1: Schlupfvariablen einführen

Um Ungleichungen in Gleichungen umzuwandeln, fügen wir **Schlupfvariablen** (slack variables) ein:

```{r echo=TRUE}
cat("4x_1 + 2x_2 + s_1 = 40    (s_1 >= 0)\n")
cat("10x_1 + 30x_2 + s_2 = 120  (s_2 >= 0)\n")
cat("x_1 + x_2 + s_3 = 9        (s_3 >= 0)\n\n")
cat("Zielfunktion (Maximierung):\n")
cat("Z = 40x_1 + 30x_2 + 0·s_1 + 0·s_2 + 0·s_3 \n")
```

**Standardform (Matrixschreibweise):**

```
max Z = c^T · x

mit A·x = b und x >= 0

c = [40, 30, 0, 0, 0]^T
b = [40, 120, 9]^T
```

---

## Das Simplex-Tableau: Initiale Form

```{r}
# Initiales Tableau
tableau_0 <- data.frame(
  Basis = c("s_1", "s_2", "s_3", "Z"),
  x_1 = c(4, 10, 1, -40),
  x_2 = c(2, 30, 1, -30),
  s_1 = c(1, 0, 0, 0),
  s_2 = c(0, 1, 0, 0),
  s_3 = c(0, 0, 1, 0),
  RHS = c(40, 120, 9, 0)
)

knitr::kable(tableau_0, caption = "Initiales Simplex-Tableau (Iteration 0)")
```

**Interpretation:**
- Jede Zeile ist eine Gleichung
- **Basislösung**: s_1=40, s_2=120, s_3=9 (x_1=0, x_2=0)
- **Z-Wert**: 0
- **Negative Einträge in Z-Reihe**: Möglichkeit zur Verbesserung

---

## Der Simplex-Algorithmus: Schritt 1

### Eingangsvariable bestimmen

**Kriterium**: Wähle die Variable mit dem **kleinsten (negativsten)** Wert in der Z-Reihe

```{r echo=TRUE}
cat("Z-Reihe: [-40, -30, 0, 0, 0]\n")
cat("\nKleinster Wert: -40 → x_1 wird Eingangsvariable\n")
cat("(Spalte 1 wird Pivotspalte)\n")
```

**Intuition**: x_1 hat den höchsten Gewinn pro Einheit → sollte zuerst erhöht werden

---

## Der Simplex-Algorithmus: Schritt 2

### Ausgangsvariable bestimmen (Minimum-Ratio-Test)

**Kriterium**: Minimaler Quotient RHS/Pivotspalte (nur positive Quotienten)

```{r echo=TRUE}
cat("Quotienten für Pivotspalte x_1:\n")
cat("Zeile 1: 40/4 = 10\n")
cat("Zeile 2: 120/10 = 12\n")
cat("Zeile 3: 9/1 = 9  ← Minimum!\n\n")
cat("Ausgangsvariable: s_3\n")
cat("Pivotelement: 1 (Zeile 3, Spalte 1)\n")
```

---

## Der Simplex-Algorithmus: Schritt 3

### Pivot-Operation (Gauss-Elimination)

**Ziel**: Pivotspalte so transformieren, dass nur das Pivotelement = 1 ist

```{r echo=TRUE}
cat("Neue Zeile 3 (Pivotzeile): Zeile 3 ÷ 1\n")
cat("x_1 + x_2 + s_3 = 9\n\n")
cat("Neue Zeile 1: alte Zeile 1 - 4×(neue Zeile 3)\n")
cat("(4x_1 + 2x_2 + s_1) - 4(x_1 + x_2 + s_3) = 40 - 4(9)\n")
cat("-2x_2 + s_1 - 4s_3 = 4\n\n")
cat("Neue Zeile 2: alte Zeile 2 - 10×(neue Zeile 3)\n")
cat("(10x_1 + 30x_2 + s_2) - 10(x_1 + x_2 + s_3) = 120 - 10(9)\n")
cat("20x_2 + s_2 - 10s_3 = 30\n\n")
cat("Neue Z-Reihe: alte Z - (-40)×(neue Zeile 3)\n")
cat("Z + 40(x_1 + x_2 + s_3) = 0 + 40(9)\n")
cat("Z + 40x_2 + 40s_3 = 360\n")
```

---

## Simplex-Tableau nach Iteration 1

```{r}
tableau_1 <- data.frame(
  Basis = c("s_1", "s_2", "x_1", "Z"),
  x_1 = c(0, 0, 1, 0),
  x_2 = c(-2, 20, 1, 40),
  s_1 = c(1, 0, 0, 0),
  s_2 = c(0, 1, 0, 0),
  s_3 = c(-4, -10, 1, 40),
  RHS = c(4, 30, 9, 360)
)

knitr::kable(tableau_1, caption = "Simplex-Tableau nach Iteration 1")
```

**Fortschritt:**
- Basislösung: x_1=9, x_2=0 (Schlupfvariablen: s_1=4, s_2=30)
- **Z-Wert verbessert sich auf 360**
- Noch ein negativer Eintrag: x_2 mit -2
- Aber im x_2-Spalte nur negative oder kleine Werte → STOP bei Minimierung dieser Spalte

---

## Fortsetzung: Iteration 2

### Neue Eingangsvariable

```{r echo=TRUE}
cat("Z-Reihe (negative Einträge): [0, 40, 0, 0, 40]\n")
cat("Achtung: Positive Einträge sind gut für Maximierung!\n")
cat("Alle neuen Kandidaten positiv → OPTIMUM ERREICHT\n")
```

**Jedoch**: Prüfen wir noch einmal das Tableau:

```{r echo=TRUE}
cat("Im Tableau nach Iteration 1:\n")
cat("Reduzierten Kosten für x_2: -2 (negativ!)\n")
cat("Ratio-Test für x_2-Spalte:\n")
cat("Zeile 1: 4/-2 = -2 (ungültig, negativ)\n")
cat("Zeile 2: 30/20 = 1.5 ← Minimum (positiv)\n")
cat("Zeile 3: 9/1 = 9\n\n")
cat("Pivot: Zeile 2, spalte x_2 (Element: 20)\n")
```

---

## Komplettes Tableau nach Iteration 2

```{r echo=TRUE}
cat("Nach Pivot-Operation:\n")
cat("Neue Zeile 2 (x_2-Reihe): (20x_2 + s_2 - 10s_3 = 30) ÷ 20\n")
cat("x_2 + 0.05s_2 - 0.5s_3 = 1.5\n\n")
cat("Neue Zeile 1: (-2x_2 + s_1 - 4s_3 = 4) - (-2) × neue Zeile 2\n")
cat("s_1 + 0.1s_2 - 3s_3 = 7\n\n")
cat("Neue Zeile 3: (x_1 + x_2 + s_3 = 9) - 1 × neue Zeile 2\n")
cat("x_1 - 0.05s_2 + 0.5s_3 = 7.5\n\n")
cat("Neue Z-Reihe: Z + 40(x_2 + 0.05s_2 - 0.5s_3 = 1.5)\n")
cat("Z + 2s_2 - 20s_3 = 420\n")
```

---

## Finales Tableau (Iteration 2)

```{r}
tableau_final <- data.frame(
  Basis = c("s_1", "x_2", "x_1", "Z"),
  x_1 = c(0, 0, 1, 0),
  x_2 = c(0, 1, 0, 0),
  s_1 = c(1, 0, 0, 0),
  s_2 = c(0.10, 0.05, -0.05, 2),
  s_3 = c(-3, -0.5, 0.5, -20),
  RHS = c(7, 1.5, 7.5, 420)
)

knitr::kable(tableau_final, caption = "Finales Simplex-Tableau (optimal)")
```

**Optimale Lösung:**
- **x_1 = 7.5** (Einheiten Produkt A)
- **x_2 = 1.5** (Einheiten Produkt B)
- **Maximaler Gewinn Z = 420 EUR**

---

## Optimalitätskriterium {.build}

### Wann ist die Lösung optimal?

**Für Maximierungsprobleme:**
- OK! Alle Einträge in der Z-Reihe (außer dem RHS) sind **>= 0**
- OK! Keine negative Reduzierten Kosten

**Unser finales Tableau:**
```
Z-Reihe: [0, 0, 0, 2, -20]
```
- Einträge für x_1, x_2, s_1 sind >= 0 OK!
- Eintrag für s_3 ist -20 (negativ)

**Interpretation**: 
- s_3 = 0 (Lagerkapazität vollständig ausgenutzt)
- Reduzierten Kosten = -20 bedeutet: Weitere Lagerkapazität wäre 20 EUR wert

---

## Ressourcen-Auslastung

```{r}
# Ressourcen-Auslastung
resources <- data.frame(
  Ressource = c("Arbeitszeit", "Material", "Lager"),
  Verfügbar = c(40, 120, 9),
  Verbraucht = c(
    4*7.5 + 2*1.5,
    10*7.5 + 30*1.5,
    7.5 + 1.5
  ),
  Restkapazität = c(
    40 - (4*7.5 + 2*1.5),
    120 - (10*7.5 + 30*1.5),
    9 - (7.5 + 1.5)
  )
)

resources$Auslastung_Prozent <- round((resources$Verbraucht / resources$Verfügbar) * 100, 1)

knitr::kable(resources, caption = "Ressourcen-Auslastung bei optimaler Lösung")
```

---

## Geometrische Interpretation

```{r fig.width=8, fig.height=6}
# Zulässiger Bereich definieren
x1 <- seq(0, 15, 0.1)

# Nebenbedingungen als Linien
y1 <- (40 - 4*x1) / 2  # 4x1 + 2x2 <= 40
y2 <- (120 - 10*x1) / 30  # 10x1 + 30x2 <= 120
y3 <- 9 - x1  # x1 + x2 <= 9

# Eckpunkte des zulässigen Bereichs
corners <- data.frame(
  x1 = c(0, 0, 9, 7.5, 0),
  x2 = c(0, 4, 0, 1.5, 0),
  Typ = c("Start", "Ecke", "Ecke", "Optimum", "Start")
)

# Gewinn für jeden Eckpunkt
corners$Gewinn <- 40*corners$x1 + 30*corners$x2

ggplot() +
  # Nebenbedingungen
  geom_line(aes(x=x1, y=y1), color="blue", linetype="dashed", alpha=0.6, size=0.7) +
  geom_line(aes(x=x1, y=y2), color="green", linetype="dashed", alpha=0.6, size=0.7) +
  geom_line(aes(x=x1, y=y3), color="red", linetype="dashed", alpha=0.6, size=0.7) +
  
  # Zulässiger Bereich
  geom_ribbon(aes(x=x1, ymin=0, ymax=pmin(y1,y2,y3,9)), 
              fill="lightblue", alpha=0.3) +
  
  # Eckpunkte
  geom_point(data=corners, aes(x=x1, y=x2, color=Typ, size=Typ)) +
  geom_text(data=corners, aes(x=x1, y=x2, label=paste0("(", x1, ", ", x2, ")\nZ=", Gewinn)),
            vjust=-0.8, hjust=-0.2, size=3.5) +
  
  # Iso-Gewinnlinie durch Optimum
  geom_abline(intercept=14, slope=-40/30, color="purple", size=1, alpha=0.8) +
  
  scale_color_manual(values=c("Start"="gray", "Ecke"="orange", "Optimum"="red")) +
  scale_size_manual(values=c("Start"=2, "Ecke"=3, "Optimum"=5)) +
  
  xlim(0, 13) + ylim(0, 8) +
  labs(title="Geometrische Interpretation der Linearen Optimierung",
       x="x_1 (Produkt A)",
       y="x_2 (Produkt B)",
       color="Punkt-Typ", size="Punkt-Typ") +
  theme_minimal() +
  theme(plot.title=element_text(size=14, face="bold"),
        axis.title=element_text(size=12),
        legend.position="right")
```

---

## Zusammenfassung: Simplex-Verfahren

### Der Algorithmus in 5 Schritten

1. **Standardform** → Schlupfvariablen, Maximierung
2. **Initiales Tableau** → Basislösung: alle ursprünglichen Variablen = 0
3. **Eingangsvariable** → Kleinster Wert in Z-Reihe
4. **Ausgangsvariable** → Minimum-Ratio-Test (RHS/Pivotspalte)
5. **Pivot-Operation** → Gauss-Elimination, zurück zu Schritt 3

### Merkmale

| Aspekt | Detail |
|--------|--------|
| **Komplexität** | O(m·n) im Durchschnitt, O(2^n) worst-case |
| **Güte** | Findet immer Optimum (wenn existent) |
| **Eckpunkte** | Bewegt sich entlang der Ecken des zulässigen Bereichs |
| **Konvergenz** | Endlich viele Iterationen (unter Nondegeneracy) |

---

## Praktische R-Implementierung

```{r echo=TRUE}
# Simplex mit lpSolve Paket (vereinfacht)
library(lpSolve)

# Zielfunktion: max 40x1 + 30x2
c <- c(40, 30)

# Nebenbedingungen (Constraint-Matrix)
A <- matrix(c(4, 2,        # Arbeitszeit
              10, 30,      # Material
              1, 1),       # Lager
            nrow=3, byrow=TRUE)
b <- c(40, 120, 9)
signs <- c("<=", "<=", "<=")

# Lösen
result <- lp("max", c, A, signs, b)

cat("Optimale Lösung:\n")
cat("x_1 =", result$solution[1], "\n")
cat("x_2 =", result$solution[2], "\n")
cat("Maximaler Gewinn Z =", result$objval, "EUR\n")
```

---

## Weiterführende Themen

### Variationen und Erweiterungen

- **Minimierungsprobleme** → Koeffizientenumkehrung oder Kostenvariablen
- **Künstliche Variablen** → Wenn keine triviale Basislösung verfügbar (Big-M oder Zwei-Phasen-Simplex)
- **Degenerative Lösungen** → Mehrdeutige Optima, Zyklusschutz (Bland's Rule)
- **Dualität** → Duales Problem, Komplementärer Schlupf
- **Sensitivitätsanalyse** → Robustheit der Lösung gegen Parameteränderungen
- **Spezialfall: Transportproblem** → Vogel's Approximation Method

---

## Quellen & Weitere Ressourcen {.smaller}

### Fachliteratur
- Vanderbei, R. J. (2020). *Linear Programming: Foundations and Extensions*. Springer.
- Gärtner, B., & Matoušek, J. (2006). *Understanding and Using Linear Programming*. Springer.

### Online-Ressourcen
- NEOS Solver: https://www.neos-server.org/
- R Packages: `lpSolve`, `linprog`, `Rcplex`
- Interaktive Visualization: http://simplex.tode.cz/

### Deutsch
- Kellerer, H., & Pferschy, U. (2015). *Optimierung*. Springer (German edition)

---

## Fragen?

```{r}
# Schöne Abschlussgrafik
set.seed(42)
simplex_summary <- data.frame(
  Iteration = 0:2,
  x1 = c(0, 9, 7.5),
  x2 = c(0, 0, 1.5),
  Gewinn = c(0, 360, 420)
)

ggplot(simplex_summary, aes(x=Iteration, y=Gewinn)) +
  geom_line(color="darkblue", size=1) +
  geom_point(color="red", size=4) +
  geom_text(aes(label=paste0("Z = ", Gewinn, " EUR")), 
            vjust=-0.8, hjust=0.5, size=4) +
  labs(title="Konvergenz des Simplex-Algorithmus",
       y="Zielfunktionswert [EUR]") +
  theme_minimal() +
  theme(plot.title=element_text(size=14, face="bold", hjust=0.5),
        axis.title=element_text(size=12))
```
